import tkinter as tk
from calculator import calc_cross_section

def launch_gui():
    window = tk.Tk()
    window.title("Angular Distribution Calculator")

    # GUI elements here
    # On 'Compute' button press:
    # call compute_differential_cross_section()

    window.mainloop()
